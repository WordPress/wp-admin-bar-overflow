/**
 * Mirror cloning. Real implementation lands in A.1 work item 3: clones
 * classified-as-plugin originals into the dropdown panel, renames their
 * ids to `mirror-<original>`, and attaches `data-mirror-of` pointing at
 * the original. The entry-checkpoint prototype emits the trigger +
 * placeholder shell only; the real cloning is gated on the 7-item gate
 * passing.
 */

export function setupMirror() {
	// Stub. The full clone implementation lands once the entry-checkpoint
	// measurements are green.
}
